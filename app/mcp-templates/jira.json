{
  "name": "jira",
  "description": "Jira integration — multi-instance routing, ADF formatting, task caching, and comment templates",
  "mcpServers": {
    "jira": {
      "command": "npx",
      "args": ["-y", "@softspark/jira-mcp"]
    }
  },
  "postInstall": "Run 'npx @softspark/jira-mcp config init' to set up credentials and project mappings."
}
