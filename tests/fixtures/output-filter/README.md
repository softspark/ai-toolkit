# Output-filter fixture provenance

All cases are synthetic ai-toolkit fixtures authored for the native,
dependency-free implementation. They are not copied, translated, or derived
from an external output-filter project.

Each `case.json` records its profile, expected acceptance decision, exact input
and output, origin, and mandatory facts. The property test treats these files
as the independent golden oracle.
